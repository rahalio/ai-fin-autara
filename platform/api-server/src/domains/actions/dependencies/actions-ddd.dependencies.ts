/**
 * Actions DDD Dependencies - Composition root
 *
 * Generated by Dependencies Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: actions
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * Actions Domain Module - Composition root
 *
 * DDD: Composition root for actions domain. Exposes repos (ports) and use cases
 * grouped by aggregate. No orgId at build time — use execution context at call time.
 */

import {
  ActionRepositoryAdapter,
  AutomationRepositoryAdapter,
  PauseRepositoryAdapter,
  ResumeRepositoryAdapter,
  ReverseRepositoryAdapter,
} from "@autara/adapters/actions";
import { getIdGeneratorService } from "@autara/adapters";
import type { AdapterDynamoDBClient } from "@autara/adapters";
import { executionContextService } from "../../../lib/execution-context.service.js";
// Use-case constructors (concrete)
import {
  ExecuteGetAutomationProfile,
  ExecuteGetExecutedAction,
  ExecuteListExecutedActions,
  ExecutePauseAutomation,
  ExecuteResumeAutomation,
  ExecuteReverseExecutedAction,
} from "@autara/services/actions/usecases";
// Ports (interfaces)
import type {
  ActionRepository,
  AutomationRepository,
  PauseRepository,
  ResumeRepository,
  ReverseRepository,
} from "@autara/services/actions/ports";

export interface ActionsDomainModule {
  repos: {
    actions: ActionRepository;
    automations: AutomationRepository;
    pauses: PauseRepository;
    resumes: ResumeRepository;
    reverses: ReverseRepository;
  };
  useCases: {
    actions: {
      get: ExecuteGetExecutedAction;
      list: ExecuteListExecutedActions;
    };
    automations: {
      get: ExecuteGetAutomationProfile;
    };
    pauses: {
      get: ExecutePauseAutomation;
    };
    resumes: {
      get: ExecuteResumeAutomation;
    };
    reverses: {
      get: ExecuteReverseExecutedAction;
    };
  };
}

export function buildActionsDomainModule(
  dynamoClient: AdapterDynamoDBClient,
): ActionsDomainModule {
  // Adapters implementing ports (adapters encapsulate Ddb)
  const repos = (() => {
    const obj: {
      actions?: ActionRepository;
      automations?: AutomationRepository;
      pauses?: PauseRepository;
      resumes?: ResumeRepository;
      reverses?: ReverseRepository;
    } = {};

    obj.actions = new ActionRepositoryAdapter(dynamoClient);
    obj.automations = new AutomationRepositoryAdapter(dynamoClient);
    obj.pauses = new PauseRepositoryAdapter(dynamoClient);
    obj.resumes = new ResumeRepositoryAdapter(dynamoClient);
    obj.reverses = new ReverseRepositoryAdapter(dynamoClient);
    return {
      actions: obj.actions!,
      automations: obj.automations!,
      pauses: obj.pauses!,
      resumes: obj.resumes!,
      reverses: obj.reverses!,
    };
  })();

  const executionContext = executionContextService;
  const idGenerator = getIdGeneratorService();

  // Use cases depend only on ports (repos.*)
  const useCases = {
    actions: {
      get: new ExecuteGetExecutedAction(executionContext, idGenerator, repos.actions),
      list: new ExecuteListExecutedActions(executionContext, idGenerator, repos.actions),
    },
    automations: {
      get: new ExecuteGetAutomationProfile(executionContext, idGenerator, repos.automations),
    },
    pauses: {
      get: new ExecutePauseAutomation(executionContext, idGenerator, repos.pauses),
    },
    resumes: {
      get: new ExecuteResumeAutomation(executionContext, idGenerator, repos.resumes),
    },
    reverses: {
      get: new ExecuteReverseExecutedAction(executionContext, idGenerator, repos.reverses),
    },
  };
  return { repos, useCases: useCases as ActionsDomainModule["useCases"] };
}
