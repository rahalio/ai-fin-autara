/**
 * ExecutePauseAutomation - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: actions
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecutePauseAutomation Use Case
 *
 * DDD: Application use case for get Pause.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { PauseAutomationInput, PauseAutomationOutput } from "../dto/pause.dto";
import type { ExecutionContextService, IdGeneratorService } from "@autara/services/_shared/index.js";
import type { PauseRepository } from "../ports";
import { NotFoundError, ValidationError } from "../errors";

export class ExecutePauseAutomation {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly pause: PauseRepository
  ) {}

  async execute(input: PauseAutomationInput): Promise<PauseAutomationOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.actId();

    // All field names extracted from OpenAPI spec: categories
    // Validate input
    if (!input) {
      throw new ValidationError("Input is required");
    }
    if (!(input as any).customerId) {
      throw new ValidationError("customerId is required");
    }
    const customerId = (input as any).customerId;

    // Load entity via repository (pass correlation ID for response meta)
    const entity = await this.pause.pauseAutomation({ customerId: customerId, orgId: this.context.getOrgId(), correlationId } as any);
    if (!entity) {
      throw new NotFoundError(`Entity not found: ${customerId}`);
    }

    return entity as PauseAutomationOutput;
  }
}
