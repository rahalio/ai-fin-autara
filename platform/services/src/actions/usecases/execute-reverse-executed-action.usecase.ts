/**
 * ExecuteReverseExecutedAction - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: actions
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecuteReverseExecutedAction Use Case
 *
 * DDD: Application use case for get Reverse.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { ReverseExecutedActionInput, ReverseExecutedActionOutput } from "../dto/reverse.dto";
import type { ExecutionContextService, IdGeneratorService } from "@autara/services/_shared/index.js";
import type { ReverseRepository } from "../ports";
import { NotFoundError, ValidationError } from "../errors";

export class ExecuteReverseExecutedAction {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly reverse: ReverseRepository
  ) {}

  async execute(input: ReverseExecutedActionInput): Promise<ReverseExecutedActionOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.actId();

    // All field names extracted from OpenAPI spec: none
    // Validate input
    if (!input) {
      throw new ValidationError("Input is required");
    }
    if (!(input as any).actionId) {
      throw new ValidationError("actionId is required");
    }
    const actionId = (input as any).actionId;

    // Load entity via repository (pass correlation ID for response meta)
    const entity = await this.reverse.reverseExecutedAction({ actionId: actionId, orgId: this.context.getOrgId(), correlationId } as any);
    if (!entity) {
      throw new NotFoundError(`Entity not found: ${actionId}`);
    }

    return entity as ReverseExecutedActionOutput;
  }
}
