/**
 * ExecuteGetOutcomesOverview - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: outcomes
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecuteGetOutcomesOverview Use Case
 *
 * DDD: Application use case for get Overview.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { GetOutcomesOverviewInput, GetOutcomesOverviewOutput } from "../dto/overview.dto";
import type { ExecutionContextService, IdGeneratorService } from "@autara/services/_shared/index.js";
import type { OverviewRepository } from "../ports";
import { NotFoundError } from "../errors";

export class ExecuteGetOutcomesOverview {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly overview: OverviewRepository
  ) {}

  async execute(input: GetOutcomesOverviewInput): Promise<GetOutcomesOverviewOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.outId();

    // All field names extracted from OpenAPI spec: none
    // Load entity via repository (no ID field - uses input directly, pass correlation ID for response meta)
    const entity = await this.overview.getOutcomesOverview({ ...input, orgId: this.context.getOrgId(), correlationId } as any);
    if (!entity) {
      throw new NotFoundError(`Entity not found`);
    }

    return entity as GetOutcomesOverviewOutput;
  }
}
