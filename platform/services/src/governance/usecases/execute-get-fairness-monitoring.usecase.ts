/**
 * ExecuteGetFairnessMonitoring - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: governance
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecuteGetFairnessMonitoring Use Case
 *
 * DDD: Application use case for get Fairness.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { GetFairnessMonitoringInput, GetFairnessMonitoringOutput } from "../dto/fairness.dto";
import type { ExecutionContextService, IdGeneratorService } from "@autara/services/_shared/index.js";
import type { FairnessRepository } from "../ports";
import { NotFoundError } from "../errors";

export class ExecuteGetFairnessMonitoring {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly fairness: FairnessRepository
  ) {}

  async execute(input: GetFairnessMonitoringInput): Promise<GetFairnessMonitoringOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.gvnId();

    // All field names extracted from OpenAPI spec: none
    // Load entity via repository (no ID field - uses input directly, pass correlation ID for response meta)
    const entity = await this.fairness.getFairnessMonitoring({ ...input, orgId: this.context.getOrgId(), correlationId } as any);
    if (!entity) {
      throw new NotFoundError(`Entity not found`);
    }

    return entity as GetFairnessMonitoringOutput;
  }
}
