/**
 * GetActionProposal handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: proposals
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { getActionProposal } from "@autara/api-server/domains/proposals/handlers";
import type { ProposalsDDDDependencies } from "@autara/api-server/domains/proposals";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "@autara/tests/shared";
import { PROPOSALS_USE_CASES } from "../__factories__/use-cases.js";

describe("GetActionProposal", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: ProposalsDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<ProposalsDDDDependencies>(
      PROPOSALS_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 200 with entity when found", async () => {
    const entity = { id: 'mock-id', orgId } as any;
    const useCaseResult = { data: entity, meta: { correlationId: "test", timestamp: new Date().toISOString() } };
    
    vi.spyOn(mockDeps.executeGetActionProposal, "execute").mockResolvedValue(useCaseResult);
    mockRequest.params = { proposalId: entity.id };

    await getActionProposal(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeGetActionProposal.execute).toHaveBeenCalledWith(expect.objectContaining({ proposalId: entity.id }));
    expect(mockReply.code).toHaveBeenCalledWith(200);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

  it("should return error when entity not found", async () => {
    const nonExistentId = "non-existent-id";
    mockRequest.params = { proposalId: nonExistentId };
    
    vi.spyOn(mockDeps.executeGetActionProposal, "execute").mockRejectedValue(
      new Error("Entity not found")
    );

    await expect(
      getActionProposal(mockRequest, mockReply, mockDeps)
    ).rejects.toThrow();
  });

});
