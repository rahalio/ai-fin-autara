/**
 * GetExecutedAction handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: actions
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { getExecutedAction } from "@autara/api-server/domains/actions/handlers";
import type { ActionsDDDDependencies } from "@autara/api-server/domains/actions";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "@autara/tests/shared";
import { ACTIONS_USE_CASES } from "../__factories__/use-cases.js";

describe("GetExecutedAction", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: ActionsDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<ActionsDDDDependencies>(
      ACTIONS_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 200 with entity when found", async () => {
    const entity = { id: 'mock-id', orgId } as any;
    const useCaseResult = { data: entity, meta: { correlationId: "test", timestamp: new Date().toISOString() } };
    
    vi.spyOn(mockDeps.executeGetExecutedAction, "execute").mockResolvedValue(useCaseResult);
    mockRequest.params = { actionId: entity.id };

    await getExecutedAction(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeGetExecutedAction.execute).toHaveBeenCalledWith(expect.objectContaining({ actionId: entity.id }));
    expect(mockReply.code).toHaveBeenCalledWith(200);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

  it("should return error when entity not found", async () => {
    const nonExistentId = "non-existent-id";
    mockRequest.params = { actionId: nonExistentId };
    
    vi.spyOn(mockDeps.executeGetExecutedAction, "execute").mockRejectedValue(
      new Error("Entity not found")
    );

    await expect(
      getExecutedAction(mockRequest, mockReply, mockDeps)
    ).rejects.toThrow();
  });

});
